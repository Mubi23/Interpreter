# Showing the Return Value on Method Exit Interpreter

Fully working interpreter for Task1.

Use `./gradlew build` or `gradlew build` to build the project.

To run use `./gradlew run < input.txt` or `gradlew run < input.txt`
